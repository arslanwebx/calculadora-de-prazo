{
  "name": "prazo-facil",
  "version": "1.0.0",
  "private": true,
  "description": "Calculadora de prazo processual gratuita",
  "scripts": {
    "build": "node scripts/generate-sitemap.mjs && node scripts/build.mjs",
    "check": "node scripts/check.mjs && node scripts/seo-audit.mjs",
    "check:browser": "node scripts/browser-check.mjs",
    "preview": "wrangler dev",
    "sync:data": "node scripts/sync-calculator-data.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
